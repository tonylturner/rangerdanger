networks:
  it_net:
    description: "IT/Training zone that hosts portal, jump host, and students."
    cidr: 10.10.10.0/24
  dmz_net:
    description: "Buffer network with HMI/SCADA, historian, and IDS sensor."
    cidr: 10.20.20.0/24
  ot_control_net:
    description: "Primary OT control zone hosting Process PLC and IO simulators."
    cidr: 10.30.30.0/24
  ot_safety_net:
    description: "Dedicated safety network for SIS logic controllers."
    cidr: 10.40.40.0/24
